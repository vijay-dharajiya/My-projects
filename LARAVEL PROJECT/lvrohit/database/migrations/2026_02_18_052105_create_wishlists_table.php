<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('wishlists', function (Blueprint $table) {
            $table->id();
            $table->foreignId('user_id')     //foreignId() → creates BIGINT UNSIGNED
            ->constrained()                  //->constrained() automatically references:user_id → users.id
            ->cascadeOnDelete();            //cascadeOnDelete() → deletes cart items if user/product is deleted    

            $table->foreignId('product_id')->constrained()->cascadeOnUpdate();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('wishlists');
    }
};
